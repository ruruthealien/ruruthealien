#include<stdio.h>
void main()
{
    int n, rev=0,d;
    printf("Enter the number:");
    scanf("%d",&n);
while (n !=0)
{
    d = n%10;
    rev = (rev*10)+d;
    n = n/10;
}
   printf("Reverse of %d is: %d",n,rev);
   
}

//Enter the number:156
//Reverse of 156 is: 651
